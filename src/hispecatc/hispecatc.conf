/** ---------------------------------------------------------------------------
 * @file     cryoscope.conf 
 * @brief    WDL configuration file for Cryoscope project
 * @author   Timothee Greffe
 * @date     2020-12-21 (created)
 * @modified
 *
 * This file needs to identify the following four files:
 *   WAVEFORM_FILE = <filename>
 *   SYSTEM_FILE   = <filename>
 *   SIGNAL_FILE   = <filename>
 *   SEQUENCE_FILE = <filename>
 *
 */

INCLUDE_FILE  = "hispecatc.def"        /* #defines and usage are self contained */
CDS_FILE      = "hispecatc.cds"        /* uses #defines from .def file          */
SIGNAL_FILE   = "hispecatc.signals"    /* #defines and usage are self contained */
WAVEFORM_FILE = "hispecatc.waveform"   /* uses #defines from .def and .signals  */
SEQUENCE_FILE = "hispecatc.seq"        /* uses #defines from .def and .waveform */
MODULE_FILE   = "hispecatc.mod"        /* #defines and usage are self contained */
MODE_FILE     = "hispecatc.modes"
